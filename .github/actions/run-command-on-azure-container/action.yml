name: 'Run Command on Azure Container'
description: 'Runs commands on an Azure Container App revision via az containerapp exec'

inputs:
  app_name:
    description: 'Azure Container App name'
    required: true
  resource_group:
    description: 'Azure resource group'
    required: true
  revision:
    description: 'Target revision name (auto-detected if not provided)'
    required: false
    default: ''
  commands:
    description: 'Commands to run (newline separated)'
    required: true
  description:
    description: 'Description for logging'
    required: false
    default: 'commands'
  fail_on_error:
    description: 'Whether to fail on command error'
    required: false
    default: 'true'

runs:
  using: 'composite'
  steps:
    - name: Run commands
      shell: bash
      run: |
        echo "⚙️  Running ${{ inputs.description }}..."

        APP="${{ inputs.app_name }}"
        RG="${{ inputs.resource_group }}"
        REVISION="${{ inputs.revision }}"
        COMMANDS="${{ inputs.commands }}"
        FAIL_ON_ERROR="${{ inputs.fail_on_error }}"

        if [ -z "$COMMANDS" ]; then
          echo "💡 No ${{ inputs.description }} to run"
          exit 0
        fi

        # Get active revision if not specified
        if [ -z "$REVISION" ]; then
          echo "📋 Getting active revision..."
          REVISION=$(az containerapp show \
            --name "$APP" \
            --resource-group "$RG" \
            --query "properties.latestRevisionName" -o tsv)
          echo "📝 Using revision: $REVISION"
        fi

        while IFS= read -r command; do
          if [ -n "$command" ]; then
            echo "Executing: $command"
            
            set +e
            script -e -c "az containerapp exec \
              --name \"$APP\" \
              --resource-group \"$RG\" \
              --revision \"$REVISION\" \
              --command \"$command\"" /tmp/typescript
            EXIT_CODE=$?
            set -e
            
            cat /tmp/typescript
            
            if grep -q '"Error":\s*{' /tmp/typescript || grep -q 'ERROR:' /tmp/typescript; then
              echo "❌ Error detected in command output"
              rm -f /tmp/typescript
              if [ "$FAIL_ON_ERROR" = 'true' ]; then
                exit 1
              else
                echo "⚠️  Command failed but continuing: $command"
              fi
            elif [ $EXIT_CODE -ne 0 ]; then
              echo "❌ Command exited with code: $EXIT_CODE"
              rm -f /tmp/typescript
              if [ "$FAIL_ON_ERROR" = 'true' ]; then
                exit $EXIT_CODE
              else
                echo "⚠️  Command failed but continuing: $command"
              fi
            fi
            
            rm -f /tmp/typescript
          fi
        done <<< "$COMMANDS"

        echo "✅ Finished running ${{ inputs.description }}"
